Thank you for installing {{ .Chart.Name }}!

Your application has been deployed to namespace: {{ .Release.Namespace }}

To access App Builder:
1. Open the Olares Desktop
2. Click on the "App Builder" icon
3. Start creating your custom applications!

App Builder allows you to:
- Import apps from GitHub repositories
- Auto-detect Docker configurations
- Generate complete Helm charts
- Download ready-to-deploy packages

For more information:
- Documentation: https://github.com/beclab/Olares
- Support: https://github.com/beclab/Olares/issues

To check the deployment status:
  kubectl get pods -n {{ .Release.Namespace }} | grep {{ include "olares-app-builder.fullname" . }}

To view logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "olares-app-builder.name" . }}
